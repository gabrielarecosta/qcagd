import { Product, ProductCategory } from '../types';

export const mockProductsLarge: Product[] = [];
export const TOTAL_MOCK_PRODUCTS = 0;
export function searchLargeProducts(
  query: string,
  categoria?: ProductCategory
): Product[] {
  return [];
}
