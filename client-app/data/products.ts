import { Product } from '../types';

export const mockProducts: Product[] = [];
export function searchProducts(query: string, categoria?: string): Product[] {
  return [];
}
export const featuredProducts: Product[] = [];
